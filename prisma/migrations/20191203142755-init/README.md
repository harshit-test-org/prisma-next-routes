# Migration `20191203142755-init`

This migration has been generated by Harshit Pant at 12/3/2019, 2:27:55 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."User" (
  "id" TEXT NOT NULL   ,
  "name" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20191203142755-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,14 @@
+datasource sqlite {
+  provider = "sqlite"
+  url      = "file:dev.db"
+}
+
+generator photonjs {
+  provider = "photonjs"
+}
+
+model User {
+  id   String @id @default(cuid())
+  name String
+}
+
```

## Photon Usage

You can use a specific Photon built for this migration (20191203142755-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191203142755-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
